# Copy to .env and set your values. Used for local dev and by the API (quote emails).

# --- Quote request emails (standalone site) ---
# Email address that receives "Request a quote" submissions from the finder.
QUOTE_NOTIFICATION_EMAIL=your@email.com

# Optional: "From" address for those emails (default: noreply@<your-host>).
# QUOTE_MAIL_FROM=noreply@yourdomain.com

# --- Optional: database, AI, etc. ---
# GEMINI_API_KEY=
# DB_HOST=
# DB_NAME=
# DB_USER=
# DB_PASS=
